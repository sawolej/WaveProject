{
  "name": "wave",
  "version": "0.1.0",
  "description": "game",
  "main": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "snowpack build && echo \"host: localhost:8080/build/\" && live-server build",
    "build": "snowpack build",
    "watch": "snowpack build --watch",
    "dev": "snowpack dev"
  },
  "devDependencies": {
    "snowpack": "^3.8.8",
    "typescript": "^4.8.3"
  },
  "dependencies": {}
}
